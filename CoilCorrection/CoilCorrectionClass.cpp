/*----- PROTECTED REGION ID(CoilCorrectionClass.cpp) ENABLED START -----*/
static const char *RcsId      = "$Id:  $";
static const char *TagName    = "$Name:  $";
static const char *CvsPath    = "$Source:  $";
static const char *SvnPath    = "$HeadURL:  $";
static const char *HttpServer = "http://www.esrf.eu/computing/cs/tango/tango_doc/ds_doc/";
//=============================================================================
//
// file :        CoilCorrectionClass.cpp
//
// description : C++ source for the CoilCorrectionClass. A singleton
//               class derived from DeviceClass. It implements the
//               command list and all properties and methods required
//               by the «name» once per process.
//
// project :     .
//
// $Author:  $
//
// $Revision:  $
// $Date:  $
//
// SVN only:
// $HeadURL:  $
//
// CVS only:
// $Source:  $
// $Log:  $
//
//=============================================================================
//                This file is generated by POGO
//        (Program Obviously used to Generate tango Object)
//=============================================================================


#include <CoilCorrectionClass.h>

/*----- PROTECTED REGION END -----*/

//-------------------------------------------------------------------
/**
 *	Create CoilCorrectionClass singleton and
 *	return it in a C function for Python usage
 */
//-------------------------------------------------------------------
extern "C" {
#ifdef _TG_WINDOWS_

__declspec(dllexport)

#endif

	Tango::DeviceClass *_create_CoilCorrection_class(const char *name) {
		return CoilCorrection_ns::CoilCorrectionClass::init(name);
	}
}


namespace CoilCorrection_ns
{


//===================================================================
//	Initialize pointer for singleton pattern
//===================================================================
CoilCorrectionClass *CoilCorrectionClass::_instance = NULL;

//--------------------------------------------------------
/**
 * method : 		CoilCorrectionClass::CoilCorrectionClass(string &s)
 * description : 	constructor for the CoilCorrectionClass
 *
 * @param s	The class name
 */
//--------------------------------------------------------
CoilCorrectionClass::CoilCorrectionClass(string &s):DeviceClass(s)
{
	cout2 << "Entering CoilCorrectionClass constructor" << endl;
	set_default_property();
	get_class_property();
	write_class_property();

	/*----- PROTECTED REGION ID(CoilCorrection::Class::constructor) ENABLED START -----*/

	/*----- PROTECTED REGION END -----*/	//	CoilCorrection::Class::constructor

	cout2 << "Leaving CoilCorrectionClass constructor" << endl;
}


//--------------------------------------------------------
/**
 * method : 		CoilCorrectionClass::~CoilCorrectionClass()
 * description : 	destructor for the CoilCorrectionClass
 */
//--------------------------------------------------------
CoilCorrectionClass::~CoilCorrectionClass()
{
	/*----- PROTECTED REGION ID(CoilCorrection::Class::destructor) ENABLED START -----*/

	/*----- PROTECTED REGION END -----*/	//	CoilCorrection::Class::destructor

	_instance = NULL;
}


//--------------------------------------------------------
/**
 * method : 		CoilCorrectionClass::init
 * description : 	Create the object if not already done.
 *                  Otherwise, just return a pointer to the object
 *
 * @param	name	The class name
 */
//--------------------------------------------------------
CoilCorrectionClass *CoilCorrectionClass::init(const char *name)
{
	if (_instance == NULL)
	{
		try
		{
			string s(name);
			_instance = new CoilCorrectionClass(s);
		}
		catch (bad_alloc)
		{
			throw;
		}		
	}		
	return _instance;
}

//--------------------------------------------------------
/**
 * method : 		CoilCorrectionClass::instance
 * description : 	Check if object already created,
 *                  and return a pointer to the object
 */
//--------------------------------------------------------
CoilCorrectionClass *CoilCorrectionClass::instance()
{
	if (_instance == NULL)
	{
		cerr << "Class is not initialised !!" << endl;
		exit(-1);
	}
	return _instance;
}




//===================================================================
//	Command execution method calls
//===================================================================
//--------------------------------------------------------
/**
 * method : 		OnClass::execute()
 * description : 	method to trigger the execution of the command.
 *
 * @param	device	The device on which the command must be executed
 * @param	in_any	The command input data
 *
 *	returns The command output data (packed in the Any object)
 */
//--------------------------------------------------------
CORBA::Any *OnClass::execute(Tango::DeviceImpl *device, const CORBA::Any &in_any)
{
	cout2 << "OnClass::execute(): arrived" << endl;

	
	((static_cast<CoilCorrection *>(device))->on());
	return new CORBA::Any();
}
//--------------------------------------------------------
/**
 * method : 		OffClass::execute()
 * description : 	method to trigger the execution of the command.
 *
 * @param	device	The device on which the command must be executed
 * @param	in_any	The command input data
 *
 *	returns The command output data (packed in the Any object)
 */
//--------------------------------------------------------
CORBA::Any *OffClass::execute(Tango::DeviceImpl *device, const CORBA::Any &in_any)
{
	cout2 << "OffClass::execute(): arrived" << endl;

	
	((static_cast<CoilCorrection *>(device))->off());
	return new CORBA::Any();
}
//--------------------------------------------------------
/**
 * method : 		LoadClass::execute()
 * description : 	method to trigger the execution of the command.
 *
 * @param	device	The device on which the command must be executed
 * @param	in_any	The command input data
 *
 *	returns The command output data (packed in the Any object)
 */
//--------------------------------------------------------
CORBA::Any *LoadClass::execute(Tango::DeviceImpl *device, const CORBA::Any &in_any)
{
	cout2 << "LoadClass::execute(): arrived" << endl;

	Tango::DevString	argin;
	extract(in_any, argin);
	((static_cast<CoilCorrection *>(device))->load(argin));
	return new CORBA::Any();
}
//--------------------------------------------------------
/**
 * method : 		RunClass::execute()
 * description : 	method to trigger the execution of the command.
 *
 * @param	device	The device on which the command must be executed
 * @param	in_any	The command input data
 *
 *	returns The command output data (packed in the Any object)
 */
//--------------------------------------------------------
CORBA::Any *RunClass::execute(Tango::DeviceImpl *device, const CORBA::Any &in_any)
{
	cout2 << "RunClass::execute(): arrived" << endl;

	
	((static_cast<CoilCorrection *>(device))->run());
	return new CORBA::Any();
}




//===================================================================
//	Properties management
//===================================================================

//--------------------------------------------------------
/**
 * method : 		CoilCorrectionClass::get_class_property
 * description : 	Get the class property for specified name.
 *
 * @param	name  The property name
 */
//--------------------------------------------------------
Tango::DbDatum CoilCorrectionClass::get_class_property(string &prop_name)
{
	for (unsigned int i=0 ; i<cl_prop.size() ; i++)
		if (cl_prop[i].name == prop_name)
			return cl_prop[i];
	//	if not found, returns  an empty DbDatum
	return Tango::DbDatum(prop_name);
}


//--------------------------------------------------------
/**
 *	Method      : CoilCorrection::CoilCorrectionClass::get_default_device_property()()
 *	Description : Return the default value for device property.
 */
//--------------------------------------------------------
Tango::DbDatum CoilCorrectionClass::get_default_device_property(string &prop_name)
{
	for (unsigned int i=0 ; i<dev_def_prop.size() ; i++)
		if (dev_def_prop[i].name == prop_name)
			return dev_def_prop[i];
	//	if not found, return  an empty DbDatum
	return Tango::DbDatum(prop_name);
}


//--------------------------------------------------------
/**
 *	Method      : CoilCorrection::CoilCorrectionClass::get_default_class_property()()
 *	Description : Return the default value for class property.
 */
//--------------------------------------------------------
Tango::DbDatum CoilCorrectionClass::get_default_class_property(string &prop_name)
{
	for (unsigned int i=0 ; i<cl_def_prop.size() ; i++)
		if (cl_def_prop[i].name == prop_name)
			return cl_def_prop[i];
	//	if not found, return  an empty DbDatum
	return Tango::DbDatum(prop_name);
}


//--------------------------------------------------------
/**
 *	Method      : CoilCorrection::CoilCorrectionClass::get_class_property()
 *	Description : //	Add your own code to initialize
 */
//--------------------------------------------------------
void CoilCorrectionClass::get_class_property()
{
}


//--------------------------------------------------------
/**
 *	Method      : CoilCorrection::CoilCorrectionClass::set_default_property()
 *	Description : Set default property (class and device) for wizard.
 *	              For each property, add to wizard property name and description.
 *	              If default value has been set, add it to wizard property and.
 *	              store it in a DbDatum.
 */
//--------------------------------------------------------
void CoilCorrectionClass::set_default_property()
{
	string	prop_name;
	string	prop_desc;
	string	prop_def;
	vector<string>	vect_data;
	
	//	Set Default Class Properties

	//	Set Default Device Properties

	prop_name = "ControlBoxGap";
	prop_desc = "Link to controlbox moving gap axes.";
	prop_def  = "";
	vect_data.clear();
	if (prop_def.length()>0)
	{
		Tango::DbDatum	data(prop_name);
		data << vect_data ;
		dev_def_prop.push_back(data);
		add_wiz_dev_prop(prop_name, prop_desc,  prop_def);
	}
	else
		add_wiz_dev_prop(prop_name, prop_desc);

	prop_name = "ControlBoxPhase";
	prop_desc = "Link to controlbox moving phase axes.";
	prop_def  = "";
	vect_data.clear();
	if (prop_def.length()>0)
	{
		Tango::DbDatum	data(prop_name);
		data << vect_data ;
		dev_def_prop.push_back(data);
		add_wiz_dev_prop(prop_name, prop_desc,  prop_def);
	}
	else
		add_wiz_dev_prop(prop_name, prop_desc);

	prop_name = "DataFile";
	prop_desc = "Filename containing interpolation data";
	prop_def  = "";
	vect_data.clear();
	if (prop_def.length()>0)
	{
		Tango::DbDatum	data(prop_name);
		data << vect_data ;
		dev_def_prop.push_back(data);
		add_wiz_dev_prop(prop_name, prop_desc,  prop_def);
	}
	else
		add_wiz_dev_prop(prop_name, prop_desc);
}


//--------------------------------------------------------
/**
 *	Method      : CoilCorrection::CoilCorrectionClass::write_class_property()
 *	Description : Set class description fields as property in database
 */
//--------------------------------------------------------
void CoilCorrectionClass::write_class_property()
{
	//	First time, check if database used
	if (Tango::Util::_UseDb == false)
		return;

	Tango::DbData	data;
	string	classname = get_name();
	string	header;
	string::size_type	start, end;

	//	Put title
	Tango::DbDatum	title("ProjectTitle");
	string	str_title("");
	title << str_title;
	data.push_back(title);

	//	Put Description
	Tango::DbDatum	description("Description");
	vector<string>	str_desc;
	str_desc.push_back("");
	description << str_desc;
	data.push_back(description);
		
	//	put cvs or svn location
	string	filename("CoilCorrection");
	filename += "Class.cpp";
	
	// check for cvs information
	string	src_path(CvsPath);
	start = src_path.find("/");
	if (start!=string::npos)
	{
		end   = src_path.find(filename);
		if (end>start)
		{
			string	strloc = src_path.substr(start, end-start);
			//	Check if specific repository
			start = strloc.find("/cvsroot/");
			if (start!=string::npos && start>0)
			{
				string	repository = strloc.substr(0, start);
				if (repository.find("/segfs/")!=string::npos)
					strloc = "ESRF:" + strloc.substr(start, strloc.length()-start);
			}
			Tango::DbDatum	cvs_loc("cvs_location");
			cvs_loc << strloc;
			data.push_back(cvs_loc);
		}
	}
	// check for svn information
	else
	{
		string	src_path(SvnPath);
		start = src_path.find("://");
		if (start!=string::npos)
		{
			end = src_path.find(filename);
			if (end>start)
			{
				header = "$HeadURL: ";
				start = header.length();
				string	strloc = src_path.substr(start, (end-start));
				
				Tango::DbDatum	svn_loc("svn_location");
				svn_loc << strloc;
				data.push_back(svn_loc);
			}
		}
	}

	//	Get CVS or SVN revision tag
	
	// CVS tag
	string	tagname(TagName);
	header = "$Name: ";
	start = header.length();
	string	endstr(" $");
	
	end   = tagname.find(endstr);
	if (end!=string::npos && end>start)
	{
		string	strtag = tagname.substr(start, end-start);
		Tango::DbDatum	cvs_tag("cvs_tag");
		cvs_tag << strtag;
		data.push_back(cvs_tag);
	}
	
	// SVN tag
	string	svnpath(SvnPath);
	header = "$HeadURL: ";
	start = header.length();
	
	end   = svnpath.find(endstr);
	if (end!=string::npos && end>start)
	{
		string	strloc = svnpath.substr(start, end-start);
		
		string tagstr ("/tags/");
		start = strloc.find(tagstr);
		if ( start!=string::npos )
		{
			start = start + tagstr.length();
			end   = strloc.find(filename);
			string	strtag = strloc.substr(start, end-start-1);
			
			Tango::DbDatum	svn_tag("svn_tag");
			svn_tag << strtag;
			data.push_back(svn_tag);
		}
	}

	//	Get URL location
	string	httpServ(HttpServer);
	if (httpServ.length()>0)
	{
		Tango::DbDatum	db_doc_url("doc_url");
		db_doc_url << httpServ;
		data.push_back(db_doc_url);
	}

	//  Put inheritance
	Tango::DbDatum	inher_datum("InheritedFrom");
	vector<string> inheritance;
	inheritance.push_back("Device_Impl");
	inher_datum << inheritance;
	data.push_back(inher_datum);

	//	Call database and and values
	get_db_class()->put_property(data);
}




//===================================================================
//	Factory methods
//===================================================================


//--------------------------------------------------------
/**
 * method : 		CoilCorrectionClass::device_factory
 * description : 	Create the device object(s)
 *                  and store them in the device list
 *
 * @param	*devlist_ptr	The device name list
 */
//--------------------------------------------------------
void CoilCorrectionClass::device_factory(const Tango::DevVarStringArray *devlist_ptr)
{

	/*----- PROTECTED REGION ID(CoilCorrection::Class::device_factory_before) ENABLED START -----*/

	//	Add your own code

	/*----- PROTECTED REGION END -----*/	//	CoilCorrection::Class::device_factory_before

	//	Create devices and add it into the device list
	for (unsigned long i=0 ; i<devlist_ptr->length() ; i++)
	{
		cout4 << "Device name : " << (*devlist_ptr)[i].in() << endl;
		device_list.push_back(new CoilCorrection(this, (*devlist_ptr)[i]));							 
	}

	//	Manage dynamic attributes if any
	erase_dynamic_attributes(devlist_ptr, get_class_attr()->get_attr_list());

	//	Export devices to the outside world
	for (unsigned long i=1 ; i<=devlist_ptr->length() ; i++)
	{
		//	Add dynamic attributes if any
		CoilCorrection *dev = static_cast<CoilCorrection *>(device_list[device_list.size()-i]);
		dev->add_dynamic_attributes();

		//	Check before if database used.
		if ((Tango::Util::_UseDb == true) && (Tango::Util::_FileDb == false))
			export_device(dev);
		else
			export_device(dev, dev->get_name().c_str());
	}

	/*----- PROTECTED REGION ID(CoilCorrection::Class::device_factory_after) ENABLED START -----*/

	//	Add your own code

	/*----- PROTECTED REGION END -----*/	//	CoilCorrection::Class::device_factory_after

	
}


//--------------------------------------------------------
/**
 *	Method      : CoilCorrection::CoilCorrectionClass::attribute_factory()
 *	Description : Create the attribute object(s)
 *	              and store them in the attribute list
 */
//--------------------------------------------------------
void CoilCorrectionClass::attribute_factory(vector<Tango::Attr *> &att_list)
{
	/*----- PROTECTED REGION ID(CoilCorrection::Class::attribute_factory_before) ENABLED START -----*/

	//	Add your own code

	/*----- PROTECTED REGION END -----*/	//	CoilCorrection::Class::attribute_factory_before


	//	Attribute : Coil1Readback
	Coil1ReadbackAttrib	*coil1readback = new Coil1ReadbackAttrib();
	Tango::UserDefaultAttrProp	coil1readback_prop;
	//	description	not set for	Coil1Readback
	//	label	not set for	Coil1Readback
	coil1readback_prop.set_unit("A");
	//	standard_unit	not set for	Coil1Readback
	//	display_unit	not set for	Coil1Readback
	//	format	not set for	Coil1Readback
	//	max_value	not set for	Coil1Readback
	//	min_value	not set for	Coil1Readback
	//	max_alarm	not set for	Coil1Readback
	//	min_alarm	not set for	Coil1Readback
	//	max_warning	not set for	Coil1Readback
	//	min_warning	not set for	Coil1Readback
	//	delta_t	not set for	Coil1Readback
	//	delta_val	not set for	Coil1Readback
	coil1readback->set_default_properties(coil1readback_prop);
	//	Not Polled
	coil1readback->set_disp_level(Tango::OPERATOR);
	//	Not memorized

	//	Coil1Readback does not fire change event
	//	Coil1Readback does not fire archive event
	//	Coil1Readback does not fire data_ready event
	att_list.push_back(coil1readback);

	//	Attribute : Coil2Readback
	Coil2ReadbackAttrib	*coil2readback = new Coil2ReadbackAttrib();
	Tango::UserDefaultAttrProp	coil2readback_prop;
	//	description	not set for	Coil2Readback
	//	label	not set for	Coil2Readback
	coil2readback_prop.set_unit("A");
	//	standard_unit	not set for	Coil2Readback
	//	display_unit	not set for	Coil2Readback
	//	format	not set for	Coil2Readback
	//	max_value	not set for	Coil2Readback
	//	min_value	not set for	Coil2Readback
	//	max_alarm	not set for	Coil2Readback
	//	min_alarm	not set for	Coil2Readback
	//	max_warning	not set for	Coil2Readback
	//	min_warning	not set for	Coil2Readback
	//	delta_t	not set for	Coil2Readback
	//	delta_val	not set for	Coil2Readback
	coil2readback->set_default_properties(coil2readback_prop);
	//	Not Polled
	coil2readback->set_disp_level(Tango::OPERATOR);
	//	Not memorized

	//	Coil2Readback does not fire change event
	//	Coil2Readback does not fire archive event
	//	Coil2Readback does not fire data_ready event
	att_list.push_back(coil2readback);

	//	Attribute : Coil3Readback
	Coil3ReadbackAttrib	*coil3readback = new Coil3ReadbackAttrib();
	Tango::UserDefaultAttrProp	coil3readback_prop;
	//	description	not set for	Coil3Readback
	//	label	not set for	Coil3Readback
	coil3readback_prop.set_unit("A");
	//	standard_unit	not set for	Coil3Readback
	//	display_unit	not set for	Coil3Readback
	//	format	not set for	Coil3Readback
	//	max_value	not set for	Coil3Readback
	//	min_value	not set for	Coil3Readback
	//	max_alarm	not set for	Coil3Readback
	//	min_alarm	not set for	Coil3Readback
	//	max_warning	not set for	Coil3Readback
	//	min_warning	not set for	Coil3Readback
	//	delta_t	not set for	Coil3Readback
	//	delta_val	not set for	Coil3Readback
	coil3readback->set_default_properties(coil3readback_prop);
	//	Not Polled
	coil3readback->set_disp_level(Tango::OPERATOR);
	//	Not memorized

	//	Coil3Readback does not fire change event
	//	Coil3Readback does not fire archive event
	//	Coil3Readback does not fire data_ready event
	att_list.push_back(coil3readback);

	//	Attribute : Coil4Readback
	Coil4ReadbackAttrib	*coil4readback = new Coil4ReadbackAttrib();
	Tango::UserDefaultAttrProp	coil4readback_prop;
	//	description	not set for	Coil4Readback
	//	label	not set for	Coil4Readback
	coil4readback_prop.set_unit("A");
	//	standard_unit	not set for	Coil4Readback
	//	display_unit	not set for	Coil4Readback
	//	format	not set for	Coil4Readback
	//	max_value	not set for	Coil4Readback
	//	min_value	not set for	Coil4Readback
	//	max_alarm	not set for	Coil4Readback
	//	min_alarm	not set for	Coil4Readback
	//	max_warning	not set for	Coil4Readback
	//	min_warning	not set for	Coil4Readback
	//	delta_t	not set for	Coil4Readback
	//	delta_val	not set for	Coil4Readback
	coil4readback->set_default_properties(coil4readback_prop);
	//	Not Polled
	coil4readback->set_disp_level(Tango::OPERATOR);
	//	Not memorized

	//	Coil4Readback does not fire change event
	//	Coil4Readback does not fire archive event
	//	Coil4Readback does not fire data_ready event
	att_list.push_back(coil4readback);

	//	Attribute : Coil1Setpoint
	Coil1SetpointAttrib	*coil1setpoint = new Coil1SetpointAttrib();
	Tango::UserDefaultAttrProp	coil1setpoint_prop;
	//	description	not set for	Coil1Setpoint
	//	label	not set for	Coil1Setpoint
	coil1setpoint_prop.set_unit("A");
	//	standard_unit	not set for	Coil1Setpoint
	//	display_unit	not set for	Coil1Setpoint
	//	format	not set for	Coil1Setpoint
	//	max_value	not set for	Coil1Setpoint
	//	min_value	not set for	Coil1Setpoint
	//	max_alarm	not set for	Coil1Setpoint
	//	min_alarm	not set for	Coil1Setpoint
	//	max_warning	not set for	Coil1Setpoint
	//	min_warning	not set for	Coil1Setpoint
	//	delta_t	not set for	Coil1Setpoint
	//	delta_val	not set for	Coil1Setpoint
	coil1setpoint->set_default_properties(coil1setpoint_prop);
	//	Not Polled
	coil1setpoint->set_disp_level(Tango::OPERATOR);
	//	Not memorized

	//	Coil1Setpoint does not fire change event
	//	Coil1Setpoint does not fire archive event
	//	Coil1Setpoint does not fire data_ready event
	att_list.push_back(coil1setpoint);

	//	Attribute : Coil2Setpoint
	Coil2SetpointAttrib	*coil2setpoint = new Coil2SetpointAttrib();
	Tango::UserDefaultAttrProp	coil2setpoint_prop;
	//	description	not set for	Coil2Setpoint
	//	label	not set for	Coil2Setpoint
	coil2setpoint_prop.set_unit("A");
	//	standard_unit	not set for	Coil2Setpoint
	//	display_unit	not set for	Coil2Setpoint
	//	format	not set for	Coil2Setpoint
	//	max_value	not set for	Coil2Setpoint
	//	min_value	not set for	Coil2Setpoint
	//	max_alarm	not set for	Coil2Setpoint
	//	min_alarm	not set for	Coil2Setpoint
	//	max_warning	not set for	Coil2Setpoint
	//	min_warning	not set for	Coil2Setpoint
	//	delta_t	not set for	Coil2Setpoint
	//	delta_val	not set for	Coil2Setpoint
	coil2setpoint->set_default_properties(coil2setpoint_prop);
	//	Not Polled
	coil2setpoint->set_disp_level(Tango::OPERATOR);
	//	Not memorized

	//	Coil2Setpoint does not fire change event
	//	Coil2Setpoint does not fire archive event
	//	Coil2Setpoint does not fire data_ready event
	att_list.push_back(coil2setpoint);

	//	Attribute : Coil3Setpoint
	Coil3SetpointAttrib	*coil3setpoint = new Coil3SetpointAttrib();
	Tango::UserDefaultAttrProp	coil3setpoint_prop;
	//	description	not set for	Coil3Setpoint
	//	label	not set for	Coil3Setpoint
	coil3setpoint_prop.set_unit("A");
	//	standard_unit	not set for	Coil3Setpoint
	//	display_unit	not set for	Coil3Setpoint
	//	format	not set for	Coil3Setpoint
	//	max_value	not set for	Coil3Setpoint
	//	min_value	not set for	Coil3Setpoint
	//	max_alarm	not set for	Coil3Setpoint
	//	min_alarm	not set for	Coil3Setpoint
	//	max_warning	not set for	Coil3Setpoint
	//	min_warning	not set for	Coil3Setpoint
	//	delta_t	not set for	Coil3Setpoint
	//	delta_val	not set for	Coil3Setpoint
	coil3setpoint->set_default_properties(coil3setpoint_prop);
	//	Not Polled
	coil3setpoint->set_disp_level(Tango::OPERATOR);
	//	Not memorized

	//	Coil3Setpoint does not fire change event
	//	Coil3Setpoint does not fire archive event
	//	Coil3Setpoint does not fire data_ready event
	att_list.push_back(coil3setpoint);

	//	Attribute : Coil4Setpoint
	Coil4SetpointAttrib	*coil4setpoint = new Coil4SetpointAttrib();
	Tango::UserDefaultAttrProp	coil4setpoint_prop;
	//	description	not set for	Coil4Setpoint
	//	label	not set for	Coil4Setpoint
	coil4setpoint_prop.set_unit("A");
	//	standard_unit	not set for	Coil4Setpoint
	//	display_unit	not set for	Coil4Setpoint
	//	format	not set for	Coil4Setpoint
	//	max_value	not set for	Coil4Setpoint
	//	min_value	not set for	Coil4Setpoint
	//	max_alarm	not set for	Coil4Setpoint
	//	min_alarm	not set for	Coil4Setpoint
	//	max_warning	not set for	Coil4Setpoint
	//	min_warning	not set for	Coil4Setpoint
	//	delta_t	not set for	Coil4Setpoint
	//	delta_val	not set for	Coil4Setpoint
	coil4setpoint->set_default_properties(coil4setpoint_prop);
	//	Not Polled
	coil4setpoint->set_disp_level(Tango::OPERATOR);
	//	Not memorized

	//	Coil4Setpoint does not fire change event
	//	Coil4Setpoint does not fire archive event
	//	Coil4Setpoint does not fire data_ready event
	att_list.push_back(coil4setpoint);


	//	Create a list of static attributes
	create_static_attribute_list(get_class_attr()->get_attr_list());

	/*----- PROTECTED REGION ID(CoilCorrection::Class::attribute_factory_after) ENABLED START -----*/

	//	Add your own code

	/*----- PROTECTED REGION END -----*/	//	CoilCorrection::Class::attribute_factory_after

}


//--------------------------------------------------------
/**
 *	Method      : CoilCorrection::CoilCorrectionClass::command_factory()
 *	Description : Create the command object(s)
 *	              and store them in the command list
 */
//--------------------------------------------------------
void CoilCorrectionClass::command_factory()
{
	/*----- PROTECTED REGION ID(CoilCorrection::Class::command_factory_before) ENABLED START -----*/

	/*----- PROTECTED REGION END -----*/	//	CoilCorrection::Class::command_factory_before

	OnClass	*pOnCmd =
		new OnClass("On",
			Tango::DEV_VOID, Tango::DEV_VOID,
			"",
			"",
			Tango::OPERATOR);
	command_list.push_back(pOnCmd);
	OffClass	*pOffCmd =
		new OffClass("Off",
			Tango::DEV_VOID, Tango::DEV_VOID,
			"",
			"",
			Tango::OPERATOR);
	command_list.push_back(pOffCmd);
	LoadClass	*pLoadCmd =
		new LoadClass("Load",
			Tango::DEV_STRING, Tango::DEV_VOID,
			"fileName",
			"",
			Tango::EXPERT);
	command_list.push_back(pLoadCmd);
	RunClass	*pRunCmd =
		new RunClass("Run",
			Tango::DEV_VOID, Tango::DEV_VOID,
			"",
			"",
			Tango::OPERATOR);
	command_list.push_back(pRunCmd);

	/*----- PROTECTED REGION ID(CoilCorrection::Class::command_factory_after) ENABLED START -----*/

	/*----- PROTECTED REGION END -----*/	//	CoilCorrection::Class::command_factory_after

}




//===================================================================
//	Dynamic attributes related methods
//===================================================================


//--------------------------------------------------------
/**
 * method : 		CoilCorrectionClass::create_static_attribute_list
 * description : 	Create the a list of static attributes
 *
 * @param	att_list	the ceated attribute list 
 */
//--------------------------------------------------------
void CoilCorrectionClass::create_static_attribute_list(vector<Tango::Attr *> &att_list)
{
	for (unsigned long i=0 ; i<att_list.size() ; i++)
	{
		string att_name(att_list[i]->get_name());
		transform(att_name.begin(), att_name.end(), att_name.begin(), ::tolower);
		defaultAttList.push_back(att_name);
	}

	cout2 << defaultAttList.size() << " attributes in default list" << endl;


	/*----- PROTECTED REGION ID(CoilCorrection::Class::create_static_att_list) ENABLED START -----*/

	/*----- PROTECTED REGION END -----*/	//	CoilCorrection::Class::create_static_att_list

}


//--------------------------------------------------------
/**
 * method : 		CoilCorrectionClass::erase_dynamic_attributes
 * description : 	delete the dynamic attributes if any.
 *
 * @param	devlist_ptr	the device list pointer
 * @param	list of all attributes
 */
//--------------------------------------------------------
void CoilCorrectionClass::erase_dynamic_attributes(const Tango::DevVarStringArray *devlist_ptr, vector<Tango::Attr *> &att_list)
{
	Tango::Util *tg = Tango::Util::instance();

	for (unsigned long i=0 ; i<devlist_ptr->length() ; i++)
	{	
		Tango::DeviceImpl *dev_impl = tg->get_device_by_name(((string)(*devlist_ptr)[i]).c_str());
		CoilCorrection *dev = static_cast<CoilCorrection *> (dev_impl);
		
		vector<Tango::Attribute *> &dev_att_list = dev->get_device_attr()->get_attribute_list();
		vector<Tango::Attribute *>::iterator ite_att;
		for (ite_att=dev_att_list.begin() ; ite_att != dev_att_list.end() ; ++ite_att)
		{
			string att_name((*ite_att)->get_name_lower());
			if ((att_name == "state") || (att_name == "status"))
				continue;
			vector<string>::iterator ite_str = find(defaultAttList.begin(), defaultAttList.end(), att_name);
			if (ite_str == defaultAttList.end())
			{
				cout2 << att_name << " is a UNWANTED dynamic attribute for device " << (*devlist_ptr)[i] << endl;
				Tango::Attribute &att = dev->get_device_attr()->get_attr_by_name(att_name.c_str());
				dev->remove_attribute(att_list[att.get_attr_idx()],true);
				--ite_att;
			}
		}
	}
	/*----- PROTECTED REGION ID(CoilCorrection::Class::erase_dynamic_attributes) ENABLED START -----*/

	/*----- PROTECTED REGION END -----*/	//	CoilCorrection::Class::erase_dynamic_attributes

}



	/*----- PROTECTED REGION ID(CoilCorrection::Class::Additional Methods) ENABLED START -----*/

	/*----- PROTECTED REGION END -----*/	//	CoilCorrection::Class::Additional Methods

} //	namespace
